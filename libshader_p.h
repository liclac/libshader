#ifndef LIBSHADER_P_H
#define LIBSHADER_P_H

// Include OpenGL Header(s)
#ifdef __APPLE__
	#include <OpenGL/gl.h>
#else
	#ifdef _WIN32
		#include <windows.h>
	#endif
	
	#include <GL/gl.h>
#endif

#endif
